# 2026-07-29

- Recorded by: actor_ken / agent_claude
- Sources: Claude Code セッション

> このファイルは Evidence の素材置き場です。判断せずそのまま書いてください。
> 確定した仕様をここに書かないでください。

## 観測 / Observation

- architecture v0.3 の 6.2 節は `content_hash` の手順を書いているが、`canonical-json-v1` の
  正規化規則そのものは定義していない。名称だけが決まっていた状態だった。
- `seed/sample.de_product_rename.v0.3.yaml` に実際に計算された
  `content_hash: f9eb852866535b176c9af8c6e9f0d9fcc18ab5950771b4969b652cd04f6acbf6` があった。
- 直列化と除外フィールドの組み合わせを網羅的に試したところ、次の 1 組だけがこの値を再現した。
  - 除外: `signatures` と `integrity.content_hash`（`previous_event_hash` は除外しない）
  - キー昇順 / 区切り `,` `:` / 非 ASCII そのまま / UTF-8 / 末尾改行なし
- `seed/sample.de_signity_rename.v0.2.yaml` の `content_hash: cab89584…` は
  同じ規則で再現できなかった。試した組み合わせのどれにも一致しない。
- Schema の `content_hash` は `^(pending|[a-f0-9]{64})$`。Draft は `pending` を入れる想定だった。
  DE-20260728-001 に書いていた `content_hash: null` は Schema 違反だった。
- Schema は `status: approved` の Event に `approved_at`、`approved_by`（1 件以上）、
  64 桁の `content_hash` を必須にしている。承認前後の区別が Schema レベルで効いている。

## 議論 / Discussion

- 正規化規則を新規に設計するか、既存ハッシュから復元するか。
  - 新規設計（RFC 8785 など）にすると正本シードの `f9eb8528…` が無効になる。
  - 過去の Event を再計算するのは「承認済み Event を編集しない」原則に反する。
  - → 復元して採用した。
- `canonical-json-v1` を将来変更できるようにするか。
  - 変更すると過去のすべての `content_hash` が無効になる。
  - → 凍結し、変更は `canonical-json-v2` として別定義することにした。
- DE-20260710-001 / 002 を Ledger に入れるか。出典に `recorded_at` がない。
  - `recorded_at` の定義は「システムへ登録した時刻」。2026-07-10 にシステムは存在しなかった。
  - → この Ledger へ追記した時刻（2026-07-28T10:16:12+09:00）を入れた。
    補った値はすべて `ledger/README.md` に明記した。
- `primary_object_id` をどうするか。出典の Markdown は `DO-0001`、正本シードは
  `do_01J2H_EXAMPLE_TRANSITION_ENGINE`（`EXAMPLE` を含む）。
  - シードを変えると `f9eb8528…` が無効になる。
  - → シードの値に揃えた。正式な ID の発行は要決定として記録した。

## 状態遷移の候補 / Decision Candidates

| # | 内容 | Decision Object | Decision Type | 判断 |
| --- | --- | --- | --- | --- |
| 1 | canonical-json-v1 の規則を既存ハッシュから復元して確定し、凍結する | Signity Transition Engine | create | **承認** → DE-20260728-002（draft・承認待ち） |
| 2 | Decision Object を成立させる `create` Event を先頭に追記するか | Signity Transition Engine | create | **要決定**（strict 投影が失敗する） |
| 3 | 正式な Object ID を発行し `do_01J2H_EXAMPLE_…` から移行するか | Signity Transition Engine | change | **要決定** |
| 4 | 2026-07-10 の ChatGPT Live 対話・朝のブリーフィングを Evidence として記録する | Signity | create | **要作業**（evidence_refs 2 件が未解決） |
| 5 | `lifecycle.stage` と `governance.human_approval_required` を Decision Event にする | Signity Transition Engine | create | **要決定**（現在は文書に手入力されている） |
| 6 | v0.2 の `content_hash` の算出方法を調べるか、検証不能として確定するか | Signity | end | 保留 |

## アクション / Action

- `docs/architecture/canonical-json-v1.md` を書いた（Normative / 凍結）。
- `tools/signity/` を実装した。
  - `canonical.py` 正規化と `content_hash`
  - `ledger.py` チェーンの構築・検証
  - `projection.py` Current State の投影
  - `validate.py` Schema 検証（外部依存なしの部分実装）
  - `cli.py` `hash` / `validate` / `chain` / `verify` / `state` / `append`
- `ledger/events/` を作り、3 件を追記した。先頭は正本シード。
- `ledger/pending/` に Draft 2 件を置いた（DE-20260728-001 / 002）。
- テスト 60 件を書いた。改ざん検知は実際に改変して確認した。
- `scripts/signity` と `scripts/verify-all.sh` を追加した。
- DE-20260728-001 の `content_hash: null` を `pending` に直した（Schema 違反だった）。

## 結果 / Outcome

- **正本ハッシュを再現できた。** Ledger 先頭の `content_hash` は
  `f9eb852866535b176c9af8c6e9f0d9fcc18ab5950771b4969b652cd04f6acbf6`。
  2026-07-10 時点の実装と本実装が一致している証拠。
- **チェーンが繋がった。** 3 件が `previous_event_hash` で連結され、`verify` が通る。
- **改ざん検知が実際に動いた。** 実際の Ledger の複製に 2 種類の改変を加えて確認した。
  - 過去 Event の値を書き換えただけ → `content_hash` 不一致で検知
  - 書き換えて `content_hash` も再計算し自己整合させた → 後続との連結が切れて検知
- **投影が動き、同時に Ledger の欠落を検出した。** strict モードで
  先頭 Event の `before` が投影開始時の値と食い違う。Decision Object を成立させる
  `create` Event がないことが機械的に判明した。
- **evidence_refs 2 件が未解決であることも機械的に判明した。**
- テスト 60 件すべて成功。`./scripts/verify-all.sh` が通る。

## 本日書き出した Decision Event

- DE-20260728-002: canonical-json-v1 の正規化規則を確定し、凍結する（status: draft / 承認待ち）
