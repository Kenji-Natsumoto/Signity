# 2026-09-09

- Recorded by: actor_ken / agent_claude
- Sources: Claude Code セッション

> このファイルは Evidence の素材置き場です。判断せずそのまま書いてください。
> 確定した仕様をここに書かないでください。

## 観測 / Observation

- 要望（10:30）: 「夜就寝したら、案件・部門ごとに連携させている Slack・メール・その他メッセンジャー・
  SNS の情報と、Mac に格納されているその日決まったこと・決まっていないこと・議事録をすべてスキャンして、
  最新の情報を確定して記録する。これを毎日繰り返すループを作りたい。」
- `docs/journal/` には 2026-07-28 と 2026-07-29 の 2 日分しかない。
  DE-20260728-001 の success_metrics「2 週間で journal が 10 営業日分蓄積されている」は未達。
  同 Event の revisit_triggers に「日次 journal の運用が 2 週間継続できなかったとき」がある。
- Claude Code Remote に接続済みのコネクタは Slack / Gmail / Google Calendar / Google Drive / GitHub。
- LINE / Chatwork / Teams / Discord / X / Facebook / Notion のコネクタは接続されていない。
- Claude Code Remote は隔離コンテナで動く。**Mac のファイルシステムには到達できない。**
  設定で解決できる制約ではない。
- 同じ環境に朝のブリーフィング Routine が既にある（cron `0 21 * * 0-4` = JST 06:00 平日、
  2026-07-27 作成、直近の last_run は SUCCEEDED）。夜間 Routine の前例として使える。
- `docs/README.md` 3 章の日次運用フローは 5 段階。1〜3 は観測、4 は承認、5 は反映。
- `AGENTS.md` 3 章: 「人間の承認なしに Decision Event を確定しない。AI ができるのは候補の提示と
  Draft の生成までで、`status: approved` を自分で付けない。」
- このリポジトリに `main` ブランチはない。既定ブランチ（origin/HEAD）は
  `claude/signity-document-accumulation-qgllub`。毎晩の自動コミットの行き先として
  そのままでよいかは未決。

## 議論 / Discussion

- 「最新の情報を確定して記録する」の「確定」をどう解釈するか。
  - 決定まで確定して `ledger/events/` に追記すると、Signity の中核原則（人間承認必須）に反する。
  - → 確定度を 3 層に分けた。L1 Observed（AI が確定してよい）/ L2 Candidate（AI は提示のみ）/
    L3 Approved（人間のみ）。ループが確定するのは L1 だけ。
- 「決まったこと」だけを出すか、三分類にするか。
  - 実務で滞留を生むのは「決まったつもりで、主体・期日・範囲のどれかが欠けているもの」。
  - → 「決まったこと / 決まっていないこと / 決まったつもりのもの」の三分類にし、
    3 つ目の可視化を主目的とした。横断で「返していない球」も出す。
- Mac のローカル情報をどう取るか。
  - リモートから直接は取れない。Drive 同期・ローカルエージェント・push の 3 案。
  - → Mac 側の launchd（22:20 JST）が当日更新ファイルを `docs/journal/inbox/` へ push する方式にした。
    22:30 に起動するリモートのループが、その push を読む。
- コネクタのないメッセンジャー / SNS をどうするか。
  - → 「読めないものを読めたことにしない」。通知メール転送（Gmail ラベル `Signity/Inbox`）か
    inbox への手動投下で橋渡しし、取得できなかったソースは `scan.json` に
    `status: unavailable` として必ず記録する。
- 業務の生ログを Git に蓄積してよいか。
  - → よくない。`AGENTS.md` 3 章の延長として、journal には参照（permalink / message id / file id）と
    要約だけを書く。機微な記述は `[機微：参照のみ]` として参照だけ残す。
- 起動時刻。要望末尾の「10:30」が就寝時刻（22:30）なのか、発言時刻（10:30）なのかが判然としない。
  - → 22:30 JST（cron `30 13 * * *` UTC）を仮置きとして仕様に書いた。**人間の確認待ち。**

## 状態遷移の候補 / Decision Candidates

| # | 内容 | Decision Object | Decision Type | 判断 |
| --- | --- | --- | --- | --- |
| 1 | 夜間キャプチャループを Capture 層として導入する | Signity | create | **要承認** → DE-20260909-001（draft） |
| 2 | 起動時刻を 22:30 JST に確定する | Signity | create | **要決定**（10:30 / 22:30 の解釈が未確定） |
| 3 | Routine の実行を実際に開始（arm）するか | Signity | create | **要決定**（人間の承認待ち。未登録） |
| 4 | 案件 / 部門を Decision Object として定義するか | Signity | create | **要決定**（現在はチャンネル名からの推定） |
| 5 | inbox 素材を `docs/evidence/` へ昇格させる判断を誰がいつ行うか | Signity | create | 保留 |
| 6 | inbox の保持期間・掃除方針 | Signity | create | 保留 |
| 7 | 既定ブランチを `main` にするか、現状の `claude/signity-document-accumulation-qgllub` のままにするか | Signity | create | **要決定**（毎晩の自動コミットの行き先） |

## アクション / Action

- `docs/development/nightly-capture-loop.md` を書いた（運用仕様。確定度 3 層 / 6 フェーズ /
  対象ソース / 秘密情報の扱い / 冪等性 / 失敗モード）。
- `docs/development/prompts/nightly-capture-prompt.md` を書いた（Routine の無人実行プロンプト正本）。
  Routine はリポジトリ外に保存されるため、正本を Git に置いて履歴に残す方針にした。
- `docs/development/prompts/README.md` を追加した。
- `templates/nightly-scan-section.md` を追加した（夜間スキャン用の journal ひな形）。
- `docs/journal/inbox/README.md` を追加した（Mac 素材・未接続ソースの受け口）。
- `scripts/new-journal.sh` を追加した（当日の journal を冪等に用意する）。
- `scripts/mac-collect.sh` を追加した（Mac 側コレクタ。秘密ファイルの除外・サイズ上限・
  `_manifest.yaml` の記録・push の指数バックオフ再試行を含む）。
- `scripts/launchd/net.sprintjapan.signity.collect.plist` を追加した（22:20 起動）。
- `ledger/pending/DE-20260909-001.json` に Draft を作った（`status: draft` / 承認待ち）。
- 各ディレクトリの README の索引を更新した。

## 結果 / Outcome

- `./scripts/signity validate ledger/pending/DE-20260909-001.json` が通った。
- `./scripts/verify-all.sh` が通った（既知の未解決 3 件は従来どおり）。
- **Routine はまだ登録していない。** 起動時刻の確認と人間の承認が先。
  現時点でループは「仕様と道具が揃った状態」であって、稼働していない。
- Mac 側コレクタは Mac 上でしか実行できないため、このセッションでは動作確認できていない。
  構文チェック（`bash -n`）のみ通した。**未検証。**

## 本日書き出した Decision Event

- DE-20260909-001: 夜間キャプチャループを Signity の Capture 層として導入する（status: draft / 承認待ち）

---

# 2026-09-09 追記: 手動スキャン（第 1 回）

- Recorded by: agent_claude（夜間キャプチャループ / 手動試走）
- Sources: Slack / Gmail / Google Calendar / Google Drive / GitHub / inbox
- Scan window: 2026-09-09T00:00:00+09:00 〜 2026-09-09T11:03:45+09:00
- 機械可読な記録: [`2026-09-09.scan.json`](2026-09-09.scan.json)

> DE-20260909-001 の承認後、Routine を登録する前に手動で 1 回走らせたものです。
> **窓の終端は仕様の 22:30 ではなく実行時刻（11:03）です。** 本日の会議はまだ実施されていません。

## スキャン範囲 / Scan Coverage

| ソース | 取得 | 範囲 | 件数 | 備考 |
| --- | --- | --- | --- | --- |
| Slack | partial | 参加 29 件のうち 21 件（公開 5 / 非公開 9 / 人との DM 7） | 0 | 窓内の発言なし。ボット DM 8 件は未走査 |
| Gmail | ok | `newer_than:1d` のうち窓内 15 スレッド | 15 | すべて通知・広告・ニュースレター。人との往復なし |
| Google Calendar | ok | primary（Fixed）のみ | 3 | 3 件とも 14:00 以降で**未実施**。他カレンダーは未確認 |
| Google Drive | ok | lastModified 上位 15 件のうち窓内 1 件 | 1 | ジャーナリング `2026-09-09.md`（ひな形のまま・記入なし） |
| GitHub | ok | Kenji-Natsumoto/Signity 全ブランチ | 2 | `dc14fca` / `905f633` |
| inbox（Mac） | **unavailable** | — | — | `scripts/mac-collect.sh` が Mac 側に未設置。当日ディレクトリなし |
| inbox（未接続ソース） | **unavailable** | LINE / Chatwork / Teams / Discord / X / Facebook / Notion | — | 転送先ラベル `Signity/Inbox` が Gmail に**存在しない**。橋渡しが成立していない |

## 観測 / Observation

### Signity（Decision OS）

- `DE-20260909-001`（夜間キャプチャループの導入）を `actor_ken` が承認した（2026-09-09T10:58:36+09:00）。
  `ledger/events/0004-DE-20260909-001.json` として追記。`content_hash: 06be2fbb…`、
  `previous_event_hash: 5e0e8661…`。`./scripts/verify-all.sh` は通っている。
- 既定ブランチについて「`main` を作る」判断が出た。`main` を作成し `905f633` を push した。
  **GitHub 側の既定ブランチ設定（origin/HEAD）はまだ変更されていない。** エージェントの権限では変更できない。
- Routine の起動について「まず 1 回だけ手動実行する」判断が出た。**Routine は未登録のまま。**
- GitHub commit 2 件（いずれも本リポジトリ）
  - `dc14fca` 01:39Z 夜間キャプチャループの設計一式（`claude/nighttime-scan-loop-0tc3ja`）
  - `905f633` 02:00Z DE-20260909-001 の承認と既定ブランチの整理（`main` / 作業ブランチ）

### U-ALMEX / R Design

- 本日 14:00 移動、14:30 「U-ALMEX 小川部長と R Design について談話@浅草本社」、
  15:00「U-ALMEX インターン@浅草本社」。**いずれも未実施**（スキャン時点 11:03）。
  参照: Google Calendar `74tu8u2ikvrkmrh4g7942jtg8u` / `55pkfh0dsd85ung59bgtvnegna`
- 前日（窓外）に `議事録_260908.md`（file id `1-Zrf5t4WEwwvYNPvSVVWEmJ_NtWZFHMp`）と
  `20260908_UALMEX_DS定例会第3回_画面記録_SPRINT Japan.pptx.pdf`（`1TkNHcCHlVna1mdvKb0g4mnAecyFzQThj`）が
  Drive の `R Design/260908/` に作られている。**本日の窓には含まれないため、内容は読んでいない。**

### ジャーナリング

- Drive `ジャーナリング/2026-09-09.md`（file id `1CYUhwJIWdbpfRn2JvRxFaddGm2vW6aKE`、07:30 JST 作成）は
  **ひな形のまま**で、本文の記入がない。

### 外部からの通知（窓内 Gmail・すべて一次情報ではない）

- Notion: 「9 月 10 日以降、プレミアム AI モデルにはクレジットポイントが必要になります」
  （`team@mail.notion.so` / thread `1a083c9dd2ee81d0`）。**明日から効力が発生する外部の変更。**
- Ollama: Team plan を月 $500 の紹介価格で提供開始（`hello@ollama.com` / `1a0837aebcbb24b9`）。
- Intercom: 利用規約の変更通知（`teams@intercom.com` / `1a0818f54c3d9417`）。
- 残り 12 件はセミナー案内・ニュースレター・レコメンドで、判断を要するものはない。

## 決まったこと / Settled

| # | 案件 | 内容 | 主体 | 効力発生 | 参照 |
| --- | --- | --- | --- | --- | --- |
| 1 | Signity | 夜間キャプチャループを Capture 層として導入する | actor_ken | 2026-09-09T22:30+09:00（`effective_at`） | `ledger/events/0004-DE-20260909-001.json` |
| 2 | Signity | 既定ブランチを `main` にし、夜間ループの push 先を `main` に固定する | actor_ken | 2026-09-09（`main` 作成済み） | `ledger/pending/DE-20260909-002.json` |
| 3 | Signity | Routine は登録せず、まず手動で 1 回走らせて出力を見てから判断する | actor_ken | 2026-09-09 | この追記そのもの |

## 決まっていないこと / Unsettled

| # | 案件 | 論点 | 止まっている理由 | 次に必要な判断 |
| --- | --- | --- | --- | --- |
| 1 | Signity | 夜間ループの起動時刻（22:30 JST でよいか） | 要望の「10:30」が就寝時刻か発言時刻か未確定 | 起動時刻の確定 |
| 2 | Signity | Routine を登録して無人稼働を始めるか | この試走の出力を見てから判断する、とした | 試走の評価と arm の可否 |
| 3 | Signity | GitHub の既定ブランチ設定を `main` に切り替える | リポジトリ設定の変更はエージェントの権限外 | 人間が GitHub 側で実施する |
| 4 | Signity | Gmail ラベル `Signity/Inbox` を作り、未接続ソースの転送を始めるか | ラベルが未作成で橋渡しが成立していない | ラベル作成と転送設定の要否 |
| 5 | Signity | Mac 側コレクタ（launchd 22:20）を実際に設置するか | Mac 上でしか設置・検証できない | 設置の可否と検証 |

## 決まったつもりのもの / Ambiguous

| # | 案件 | 合意に見える内容 | 欠けているもの | 参照 |
| --- | --- | --- | --- | --- |
| 1 | Signity | 「夜間キャプチャループを導入した」 | **効力発生の実体**。`effective_at` は本日 22:30 だが、実行主体である Routine が登録されていない。承認済みだが、今夜は動かない | `ledger/events/0004-DE-20260909-001.json` |
| 2 | Signity | 「既定ブランチを main にした」 | **範囲**。`main` ブランチは作ったが、GitHub の既定ブランチ設定は旧ブランチのまま。誰がいつ切り替えるかが未定 | `git ls-remote --symref origin HEAD` |

## 返していない球 / Open Balls

| # | 相手 | 内容 | 経過 | 参照 |
| --- | --- | --- | --- | --- |
| 1 | Ivo G.（LinkedIn） | メッセージが届いており未返信（窓外・2026-09-08 20:31 JST の通知メールで検知） | 1 日 | Gmail thread `1a080caf778b05a8` |

窓内（本日 00:00〜11:03）に自分宛の依頼・質問は届いていない。

## 矛盾 / Contradictions

- `DE-20260909-001` の `effective_at` は 2026-09-09T22:30+09:00 だが、起動時刻は
  「決まっていないこと #1」として未確定のまま承認された。**承認済み Event の値と、
  運用上の未決が食い違っている。** 直さずに矛盾として記録する。
  訂正が必要なら Correction Event で行う。

## 議論 / Discussion

- 手動試走の窓（00:00〜11:03）は、本来の窓（00:00〜22:30）の前半しかカバーしていない。
  本日の会議 3 件と、そこで決まることは、この記録に入っていない。
  夜間に走らせる前提の仕様を昼に走らせると、**「決まったこと」が構造的に少なく出る。**
- Slack・Gmail ともに、窓内に人と人の往復が 1 件もなかった。
  初回の試走としては「三分類が空になる日」の挙動を確認できた形になる。
  ただし平日午前としては静かすぎるため、走査範囲（ボット DM 8 件・他カレンダー）の
  取りこぼしを疑う余地は残る。

## 状態遷移の候補 / Decision Candidates

| # | 内容 | Decision Object | Decision Type | 判断 |
| --- | --- | --- | --- | --- |
| 1 | 既定ブランチを `main` にし、夜間ループの push 先を固定する | Signity | change | **要承認** → DE-20260909-002（draft） |
| 2 | 夜間ループの起動時刻を確定する | Signity | change | **要決定**（22:30 は仮置き） |
| 3 | Routine を登録して無人稼働を開始する | Signity | create | **要決定**（この試走の評価待ち） |
| 4 | 未接続ソースの橋渡し（Gmail ラベル `Signity/Inbox`）を成立させる | Signity | create | **要決定** |
| 5 | Mac 側コレクタを設置する | Signity | create | **要決定** |

## 期限が近いコミットメント / Due Soon

| Display ID | 内容 | review_due_at | 残り |
| --- | --- | --- | --- |
| DE-20260909-001 | 夜間キャプチャループの導入 | 2026-09-23T00:00+09:00 | 14 日 |

`ledger/pending/` に `DE-20260728-001` / `DE-20260728-002` が承認待ちのまま残っている。

## 本日生成した Draft

- DE-20260909-002: 既定ブランチを main にし、夜間ループの push 先を main に固定する（status: draft / 承認待ち）
